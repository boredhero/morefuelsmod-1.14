# This is an example mods.toml file. It contains the data relating to the loading mods.
# There are several mandatory fields (#mandatory), and many more that are optional (#optional).
# The overall format is standard TOML format, v0.5.0.
# Note that there are a couple of TOML lists in this file.
# Find more information on toml format here:  https://github.com/toml-lang/toml
# The name of the mod loader type to load - for regular FML @Mod mods it should be javafml
modLoader="javafml" #mandatory
# A version range to match for said mod loader - for regular FML @Mod it will be the forge version
loaderVersion="[26,)" #mandatory (28 is current forge version)
# A URL to refer people to when problems occur with this mod
issueTrackerURL="https://github.com/boredhero/morefuelsmod-1.14/issues" #optional
# A list of mods - how many allowed here is determined by the individual mod loader
[[mods]] #mandatory
# The modid of the mod
modId="morefuelsmod" #mandatory
# The version number of the mod - there's a few well known ${} variables useable here or just hardcode it
version="§4§l1.7.1§r" #mandatory
 # A display name for the mod
displayName="§4§lMore Fuels Mod§r" #mandatory
# A URL to query for updates for this mod. See the JSON update specification <here>
# updateJSONURL="http://myurl.me/" #optional
# A URL for the "homepage" for this mod, displayed in the mod UI
displayURL="https://www.curseforge.com/minecraft/mc-mods/more-fuels-mod" #optional
# A file name (in the root of the mod JAR) containing a logo for display
logoFile="mf-logo.png" #optional
# A text field displayed in the mod UI
# credits="Cadiboo's Modding Tutorial, The Forge Folks, the Mojang Folks" #optional
# A text field displayed in the mod UI
authors="§4Noah Martino A.K.A boredhero§r" #optional
# The description text for the mod (multi line!) (#mandatory)
description='''
§4§oThe Premiere Mod that makes more items usable as fuel as well as adding fun fuel storage items and blocks. §lNow Officially Updated to Minecraft 1.14.4§r
'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.morefuelsmod]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[28,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
